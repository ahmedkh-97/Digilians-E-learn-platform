# Excel Week 1 — Sequence & Dependencies

## Locked learning sequence

`Advanced Formatting → Formula Fundamentals → Logic/Lookup → Statistics I → Statistics II → Statistics III → Statistical/Dynamic Functions → Filtering Workflows → Data Preparation/Tools`

## Important dependencies

- Formula-based Conditional Formatting → formula fundamentals + logical conditions
- Dynamic Named Ranges → ranges/named references
- STDEV.P → Standard Deviation prerequisite
- PERCENTILE.INC → Percentile prerequisite
- NORM.DIST / BINOM.DIST → Distribution prerequisite
- FILTER / UNIQUE / SORT / SORTBY / SEQUENCE → Array/Spill concepts
- Dynamic Filter Search → FILTER + SEARCH; ISNUMBER is source-workflow context
- IFS → IF
- Lecture 18 Text to Columns → reuses the Lecture 14 tool in a different context

## Cross-link rule

When later content reuses an earlier feature:

`Previously learned → New use case → What changes here`

Do not duplicate an entire Study explanation unless the source introduces genuinely new behavior.
