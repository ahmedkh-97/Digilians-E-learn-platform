# V0.19.1 — Profile Access & Layering Fix QA

## Reported issues

1. Sticky Navbar remained visually above the Profile backdrop.
2. Exam / Bank JSON Validator was visible to Guest users.
3. Profile actions were becoming cluttered after Backup/Analytics additions.

## Layer contract

- Navbar = 120
- Update notice = 150000
- Profile backdrop = 155000
- Profile drawer = 155100
- What's New = 160000
- Admin Login = 170000
- Backup / Restore = 175000
- LOCAL TEST banner = 180000

Therefore, Profile covers normal navigation/update UI, while higher-level modals can still open above it.

## Interaction lock

While Profile is open:
- body overflow = hidden
- `.app-shell` has `inert`
- Profile close receives focus
- backdrop click closes
- Escape closes
- close button closes
- prior focus is restored

Buttons that open another modal/private route close Profile first.

## Admin Tools

Guest/default:
- `adminProfileTools` = hidden
- Analytics = hidden
- JSON Validator = hidden

Verified Supabase Admin:
- `adminProfileTools` becomes visible
- Analytics becomes visible
- JSON Validator becomes visible
- `window.__DIGILIANS_ADMIN_VERIFIED__ = true`

Logout/session failure:
- Admin Tools are hidden
- verification flag returns false

Validator also has a hard app-side guard.

## Chromium static production-DOM QA

Production `index.html` DOM + production CSS were rendered.

Guest:
- Admin Tools hidden = true
- Validator visible = false

Profile-open:
- Navbar z-index = 120
- Update = 150000
- Backdrop = 155000
- Drawer = 155100
- What's New = 160000
- Admin Login = 170000
- Backup = 175000
- body overflow = hidden
- underlying app inert = true
- `elementFromPoint()` at Navbar center returns `drawerBackdrop`

Verified-Admin simulated state:
- Analytics visible = true
- Validator visible = true

390px:
- drawer width ≈ 370.2px
- backdrop = 390 × 844
- page-level horizontal overflow = 0

Result: PASS.

## Automated Pre-Deploy

The shipped checker validates:
- layer CSS contract
- modal semantics
- body scroll lock
- app inert state
- Escape handling
- close-before-next-modal behavior
- Admin Tools hidden-default structure
- Admin verified visibility wiring
- Validator hard guard
- all prior platform regressions

## Data protection

Only `data/changelog.json` changes among JSON files.

Educational, exam, SQL, Python, Official QBank and ranking payloads remain unchanged.

## Deployment

`RUN-PREFLIGHT.bat → TEST-LOCAL.bat → GitHub LIVE`

After V0.19.1 live verification, apply the Core Freeze and begin Excel Intake Week 1.
