# V0.20.39 — Exam Navigation & Voucher Context Fix QA

## Scope

- Keep the primary sticky navbar visible during Voucher exams.
- Derive Voucher/PL-300 activity and current-question context from `generatedFromVoucher`.
- Preserve all assessment content and ranking/timer behavior.

## Regression guards

- `tests/voucher-focus-navigator.test.mjs` rejects any Voucher Focus Mode rule that hides `.topbar`.
- `tests/exam-context.test.mjs` verifies stale `English / Module / Exam` setup breadcrumbs cannot override Voucher context.

## Manual acceptance

1. Start with `START-LOCAL.bat`.
2. Open Voucher → Data Analysis → Microsoft PL-300 and start a 25-question practice.
3. Confirm the primary navbar remains visible while solving.
4. Confirm the activity bar reads `Voucher > Data Analysis > Microsoft PL-300`.
5. Confirm `THIS QUESTION` shows `Microsoft PL-300` plus the current PL-300 topic.
6. Repeat once in Dark Mode.
